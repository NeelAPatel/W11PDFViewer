<Page
    x:Class="W11PDFViewer.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:W11PDFViewer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d"
    muxc:BackdropMaterial.ApplyToRootOrPageBackground="True"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">


    <StackPanel>
        <CommandBar x:Name="CommandBar" Background="Transparent" IsOpen="False" DefaultLabelPosition="Right">
            <CommandBar.Content>
                <Grid>
                    <TextBlock x:Name="FileName" Text="FileName"/>
                    <TextBlock x:Name="Path" Text="Path" Margin="0,19,0,-19"/>
                </Grid>
            </CommandBar.Content>

            <AppBarSeparator/>
            <AppBarButton x:Name="OpenFileButton" Click="OpenFileButton_Click" Icon="OpenFile" Label="Open"/>
        </CommandBar>


        <Grid Height="764" Width="1176">
            <Image x:Name="PdfImage" Margin="538,332,206,76"/>
        </Grid>
        
    </StackPanel>

</Page>
